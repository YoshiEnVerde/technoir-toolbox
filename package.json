{
  "name": "technoir-bot-with-sixes",
  "version": "0.1.0",
  "description": "Clone of the Technoir Bot dice roller, with a fix that allows for sixes to be rolled, and a more random dice throw",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test.js",
    "coveralls": "jest --coverage && cat ./tests/coverage/lcov.info | coveralls"
  },
  "dependencies": {
    "eris": "^0.7.2",
    "express": "^4.16.4",
    "winston": "^3.2.1"
  },
  "engines": {
    "node": "8.x"
  },
  "devDependencies": {
    "coveralls": "^3.0.3",
    "jest": "^24.1.0"
  }
}
